# Deprecated path

This directory name is a historical typo (`worflows` instead of `workflows`).

Active CI/security workflows live in:

`.github/workflows/`

The legacy files here (`check.yml`, `deploy.yml`) are **not** executed by GitHub Actions
and must not be used for production deploy from this remediation track.
